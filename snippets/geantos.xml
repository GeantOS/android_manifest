<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <remote name="geantos" fetch="https://github.com/GeantOS" revision="refs/heads/16" />
    <remote name="gitlab" fetch="https://gitlab.com/" />

    <!-- GeantOS additions -->
    <!-- manifest -->
    <project path="manifest" name="android_manifest" remote="geantos" />

    <!-- general -->
    <project path="art" name="android_art" remote="geantos" />
    <project path="bionic" name="android_bionic" remote="geantos" />

    <!-- Bootable -->
    <project path="bootable/recovery" name="android_bootable_recovery" remote="geantos" />
</manifest>